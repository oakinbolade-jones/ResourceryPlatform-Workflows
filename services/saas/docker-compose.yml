version: '3.4'

services:
  identity-server:
    build:
      context: ../../
      dockerfile: templates/service/host/IdentityServerHost/Dockerfile
  
  saa-s:
    build:
      context: ../../
      dockerfile: templates/service/host/ResourceryPlatformWorkflow.SaaS.Host/Dockerfile
    depends_on:
      - identity-server