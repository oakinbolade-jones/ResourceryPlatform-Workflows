version: '3.4'

services:
  identity-server:
    build:
      context: ../../
      dockerfile: templates/service/host/IdentityServerHost/Dockerfile
  
  projects:
    build:
      context: ../../
      dockerfile: templates/service/host/ResourceryPlatformWorkflow.Workflow.Host/Dockerfile
    depends_on:
      - identity-server